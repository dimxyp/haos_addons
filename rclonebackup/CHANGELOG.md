# Changelog

## 1.0.1

- Switched from `devices`/`privileged` to `full_access: true`, fixing `fuse device /dev/fuse not found` on mount.

## 1.0.0

- Initial release: mount Azure Blob Storage (account name / key / SAS URL) or a custom rclone remote into a Home Assistant share.
